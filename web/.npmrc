# The lock file is resolved without automatic peer installs (npm 10.9 crashes
# resolving the optional canvas peer of the test tooling). Every install -
# local, CI and Vercel - uses the same setting so npm ci matches the lock.
legacy-peer-deps=true
