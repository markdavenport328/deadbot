{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix web ci && npm --prefix web run build",
  "functions": {
    "app.py": {
      "includeFiles": "{web/dist/**}",
      "maxDuration": 60
    }
  }
}
